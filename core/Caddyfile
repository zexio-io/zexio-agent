# Caddyfile for Zexio Agent

# Default listener (optional, for admin or fallback)
:80 {
    respond "Zexio Agent Gateway"
}

# Zexio Agent Admin API (if exposed via Caddy, though usually internal)
# worker.yourinfra.com {
#     reverse_proxy localhost:3000
# }

# Projects will be appended below by the worker
